name = "new-construction-scraper"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Prevent uploading parent directory assets
assets = { directory = "public", binding = "ASSETS" }

[triggers]
crons = ["0 10 * * *"]  # Daily at 10:00 UTC = 6:00 AM ET

[vars]
SUPABASE_URL = "https://qmsbvvnffaojddysvqmd.supabase.co"

# Secret: SUPABASE_SERVICE_KEY (set via `wrangler secret put`)
